# ローカル開発サーバー構築仕様書

> **作成日**: 2025年9月30日
> **最終更新**: 2025年9月30日
> **ステータス**: ✅ 仕様確定（Phase 1実装準備完了）
> **優先度**: 最優先
> **関連**: [IMPLEMENTATION_ACTION_PLAN.md](../IMPLEMENTATION_ACTION_PLAN.md#phase-1-基盤整備week-1-2)

## 📖 概要

このドキュメントは、Lichtblickマーケ**ディレクトリ詳細**:

| ディレクトリ/ファイル    | 説明                     | S3での対応                    |
| ------------------------ | ------------------------ | ----------------------------- |
| `server/`                | サーバールート           | -                             |
| `server.js`              | Node.js HTTPサーバー実装 | -                             |
| `package.json`           | npm設定、起動スクリプト  | -                             |
| `public/`                | 静的ファイルルート       | S3バケットルート              |
| `public/extensions.json` | 拡張機能カタログ         | `s3://bucket/extensions.json` |
| `public/extensions/`     | .foxeファイル格納        | `s3://bucket/extensions/`     |
| `public/thumbnails/`     | サムネイル画像格納       | `s3://bucket/thumbnails/`     |

**モノレポ統合**:

ルート`package.json`の`workspaces`に追加：

```json
{
  "workspaces": {
    "packages": [
      "desktop",
      "packages/*",
      "packages/@types/*",
      "web",
      "benchmark",
      "server" // 🆕 マーケットプレイス開発サーバー
    ]
  }
}
```

**デプロイ方法**:ストを効率化するためのローカル開発サーバーの構築手順と仕様を定義します。

### 目的

1. **開発効率の向上**: 外部サーバーに依存せず、ローカル環境で拡張機能のテストが可能
2. **マルチバージョン対応のテスト**: 複数バージョンの拡張機能を同時に提供し、マルチバージョン機能の動作確認
3. **本番環境の再現**: S3 + CloudFront構成を想定した静的ファイル配信のシミュレーション
4. **データ構造の検証**: マルチバージョン対応`extensions.json`の構造を実際に使用してテスト

### スコープ

- Node.jsシンプルHTTPサーバーの構築方法
- マルチバージョン対応`extensions.json`のデータ構造定義
- 拡張機能ファイル（`.foxe`）の配置方法
- サムネイル画像の配置方法
- 開発環境での設定方法
- S3へのデプロイ手順
- テストデータの作成ガイドライン

## 🏗️ アーキテクチャ設計

### 本番環境との対応関係

| 環境     | サーバー             | 用途                                             |
| -------- | -------------------- | ------------------------------------------------ |
| **本番** | S3 + CloudFront      | 静的ファイル配信（extensions.json、.foxe、画像） |
| **開発** | Node.js HTTPサーバー | 静的ファイル配信（本番環境を模倣）               |

### 設計原則

- ✅ **シンプル**: 最小限の依存関係、設定不要で起動
- ✅ **本番再現**: S3の静的ファイル配信と同じ動作
- ✅ **CORS対応**: フロントエンドからのアクセスを許可
- ✅ **デプロイ容易**: publicディレクトリをそのままS3にアップロード可能

## 🎯 主要機能

### 1. Node.jsシンプルHTTPサーバー

**概要**:

- 拡張機能カタログ（`extensions.json`）と拡張機能ファイル（`.foxe`）を提供するシンプルなHTTPサーバー
- 開発環境で`http://localhost:3000`でアクセス可能
- S3 + CloudFront構成を想定した静的ファイル配信

**技術選定**:

- **Node.js v18以上** の組み込みHTTPサーバー
- 外部依存なし（純粋なNode.js標準ライブラリのみ）
- または軽量な`http-server`パッケージを使用

**提供エンドポイント**:

```
GET /extensions.json        # 拡張機能カタログ（マルチバージョン対応）
GET /extensions/*.foxe      # 拡張機能ファイル
GET /thumbnails/*.png       # サムネイル画像
GET /layouts.json           # レイアウトカタログ（将来用）
GET /layouts/*.json         # レイアウトファイル（将来用）
```

### 2. マルチバージョン対応 extensions.json

**概要**:
マルチバージョン機能をサポートする拡張機能カタログのデータ構造を定義します。S3にアップロードする本番用データと完全に同じ形式です。

**完全なデータスキーマ**:

```typescript
interface ExtensionCatalog {
  /** カタログのバージョン（将来の互換性確保用） */
  version: string;

  /** 最終更新日時（ISO 8601形式） */
  lastUpdated: string;

  /** 拡張機能リスト */
  extensions: Extension[];
}

interface Extension {
  /** 拡張機能の一意ID（publisher.name形式） */
  id: string;

  /** パブリッシャー名 */
  publisher: string;

  /** 拡張機能名（技術名） */
  name: string;

  /** 表示名 */
  displayName: string;

  /** 簡潔な説明 */
  description: string;

  /** タグリスト */
  tags: string[];

  /** ライセンス */
  license?: string;

  /** ホームページURL */
  homepage?: string;

  /** リポジトリURL */
  repository?: string;

  /** サムネイルURL（相対パスまたは絶対URL） */
  thumbnail?: string;

  /** アイコンURL（相対パスまたは絶対URL） */
  icon?: string;

  /** バージョンリスト（新しい順） */
  versions: ExtensionVersion[];
}

interface ExtensionVersion {
  /** バージョン番号（Semantic Versioning） */
  version: string;

  /** リリース日（ISO 8601形式） */
  releaseDate: string;

  /** .foxeファイルのURL（相対パスまたは絶対URL） */
  url: string;

  /** ファイルサイズ（バイト） */
  size?: number;

  /** SHA-256ハッシュ（整合性検証用、オプション） */
  sha256?: string;

  /** 変更履歴（Markdown形式） */
  changelog?: string;

  /** README（Markdown形式） */
  readme?: string;

  /** このバージョン固有の説明 */
  description?: string;

  /** 最小動作バージョン（Lichtblick本体） */
  minAppVersion?: string;

  /** 最大動作バージョン（Lichtblick本体） */
  maxAppVersion?: string;
}
```

**JSONサンプル**:

```json
{
  "version": "1.0.0",
  "lastUpdated": "2025-09-30T12:00:00Z",
  "extensions": [
    {
      "id": "lichtblick.example-extension",
      "publisher": "lichtblick",
      "name": "example-extension",
      "displayName": "Example Extension",
      "description": "An example extension for demonstration",
      "tags": ["demo", "example", "utilities"],
      "license": "MIT",
      "homepage": "https://github.com/lichtblick/example-extension",
      "repository": "https://github.com/lichtblick/example-extension",
      "thumbnail": "/thumbnails/example-extension.png",
      "versions": [
        {
          "version": "2.0.0",
          "releaseDate": "2025-09-30T10:00:00Z",
          "url": "/extensions/example-extension-2.0.0.foxe",
          "size": 524288,
          "sha256": "abc123...",
          "changelog": "# v2.0.0\n\n- Major update\n- Breaking changes",
          "readme": "# Example Extension\n\nThis is an example.",
          "minAppVersion": "2.0.0"
        },
        {
          "version": "1.0.0",
          "releaseDate": "2025-09-01T10:00:00Z",
          "url": "/extensions/example-extension-1.0.0.foxe",
          "size": 262144,
          "changelog": "# v1.0.0\n\n- Initial release",
          "readme": "# Example Extension\n\nThis is an example."
        }
      ]
    }
  ]
}
```

**設計上の注意点**:

- ✅ URLは相対パス推奨（`/extensions/...`）→ S3/CloudFrontドメインに依存しない
- ✅ バージョンは新しい順に配置（配列の先頭が最新）
- ✅ 必須フィールドは最小限に（柔軟性確保）
- ✅ ISO 8601形式の日時（タイムゾーン対応）

### 3. ディレクトリ構造

**概要**:
開発サーバーのファイル配置構造を定義します。このディレクトリ構造は、S3バケットの構造をそのまま再現しています。

**モノレポ内での配置**:
Lichtblickのモノレポ構成において、`server/`は`desktop/`、`web/`、`benchmark/`と同じルートレベルに配置します。

```
lichtblick/                            # リポジトリルート
├── desktop/                           # Electronアプリケーション
├── web/                               # Webアプリケーション
├── benchmark/                         # ベンチマークツール
├── server/                            # 🆕 マーケットプレイス開発サーバー
│   ├── package.json                   # Node.js設定（起動スクリプト含む）
│   ├── server.js                      # HTTPサーバー本体
│   ├── README.md                      # サーバー使用ガイド
│   ├── .gitignore                     # Gitignore設定
│   └── public/                        # 静的ファイルルート（S3バケットに対応）
│       ├── extensions.json            # 拡張機能カタログ
│       ├── layouts.json               # レイアウトカタログ（将来用）
│       ├── extensions/                # 拡張機能ファイル
│       │   ├── example-1.0.0.foxe
│       │   ├── example-2.0.0.foxe
│       │   └── another-1.0.0.foxe
│       ├── thumbnails/                # サムネイル画像
│       │   ├── example.png
│       │   └── another.png
│       └── layouts/                   # レイアウトファイル（将来用）
│           └── example-layout.json
├── packages/                          # 共有パッケージ
└── package.json                       # モノレポ設定
```

**ディレクトリ詳細**:

| ディレクトリ/ファイル    | 説明                     | S3での対応                    |
| ------------------------ | ------------------------ | ----------------------------- |
| `server/`                | サーバールート           | -                             |
| `server.js`              | Node.js HTTPサーバー実装 | -                             |
| `package.json`           | npm設定、起動スクリプト  | -                             |
| `public/`                | 静的ファイルルート       | S3バケットルート              |
| `public/extensions.json` | 拡張機能カタログ         | `s3://bucket/extensions.json` |
| `public/extensions/`     | .foxeファイル格納        | `s3://bucket/extensions/`     |
| `public/thumbnails/`     | サムネイル画像格納       | `s3://bucket/thumbnails/`     |

**デプロイ方法**:

```bash
# publicディレクトリをS3にそのままアップロード
aws s3 sync server/public/ s3://your-bucket-name/ --acl public-read
```

### 4. サーバー実装（server.js）

**概要**:
Node.js標準ライブラリのみを使用したシンプルなHTTPサーバー。

**主要機能**:

- ✅ 静的ファイル配信
- ✅ CORS対応
- ✅ MIMEタイプ自動判定
- ✅ 404エラーハンドリング
- ✅ ログ出力

**実装例** (概要):

```javascript
const http = require("http");
const fs = require("fs");
const path = require("path");

const PORT = 3000;
const PUBLIC_DIR = path.join(__dirname, "public");

const MIME_TYPES = {
  ".json": "application/json",
  ".foxe": "application/octet-stream",
  ".png": "image/png",
  ".jpg": "image/jpeg",
  ".svg": "image/svg+xml",
};

const server = http.createServer((req, res) => {
  // CORS対応
  res.setHeader("Access-Control-Allow-Origin", "*");
  res.setHeader("Access-Control-Allow-Methods", "GET, OPTIONS");
  res.setHeader("Access-Control-Allow-Headers", "Content-Type");

  if (req.method === "OPTIONS") {
    res.writeHead(200);
    res.end();
    return;
  }

  // 静的ファイル配信ロジック
  // ... (実装ファイルで詳細化)
});

server.listen(PORT, () => {
  console.log(`Server running at http://localhost:${PORT}`);
});
```

**詳細実装**: `server.js`ファイルで提供

### 5. package.json設定

**概要**:
npm scriptsを使用した簡単な起動方法を提供。モノレポの一部として統合されます。

```json
{
  "name": "@lichtblick/marketplace-server",
  "version": "1.0.0",
  "description": "Local development server for Lichtblick Marketplace",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
```

**起動コマンド**:

```bash
# server/ディレクトリから
cd server
npm start
# または
npm run dev

# モノレポルートから（yarn workspace経由）
yarn workspace @lichtblick/marketplace-server start
```

## 📋 実装タスク

### Phase 1: Week 1-2（完全版）

#### タスク1: プロジェクト構造作成（0.5日）

- [ ] `server/`ディレクトリ作成
- [ ] `server/public/`ディレクトリ構造作成
- [ ] `.gitignore`作成

#### タスク2: サーバー実装（1日）

- [ ] `server.js`実装
  - HTTPサーバー基本機能
  - 静的ファイル配信
  - CORS対応
  - MIMEタイプハンドリング
  - エラーハンドリング
  - ログ出力

#### タスク3: package.json設定（0.5日）

- [ ] `package.json`作成
- [ ] npm scripts設定
- [ ] Node.jsバージョン指定

#### タスク4: extensions.json作成（1日）

- [ ] マルチバージョン対応のデータ構造実装
- [ ] サンプル拡張機能データ作成（3-5個）
- [ ] バージョン情報の設定
- [ ] メタデータの充実化

#### タスク5: テストデータ準備（1日）

- [ ] サンプル.foxeファイル作成（ダミーでOK）
- [ ] サムネイル画像準備
- [ ] README/CHANGELOG内容作成

#### タスク6: ドキュメント作成（0.5日）

- [ ] `server/README.md`作成
  - セットアップ手順
  - 起動方法
  - トラブルシューティング
  - S3デプロイ手順
- [ ] 使用例の記載

#### タスク7: 動作確認（0.5日）

- [ ] ローカルサーバー起動確認
- [ ] extensions.json取得確認
- [ ] .foxeファイルダウンロード確認
- [ ] CORS動作確認
- [ ] Lichtblickからの接続テスト

**合計工数**: 5日

## 🚀 セットアップ手順

### 前提条件

- Node.js 20.0.0以上がインストールされていること（モノレポの要件に準拠）
- Yarn 3.6.3以上（モノレポのpackageManager）
- Git（リポジトリクローン用）

### インストール手順

```bash
# 1. リポジトリルートからserver/ディレクトリに移動
cd server

# 2. 依存関係のインストール（モノレポの場合、ルートでyarn installすれば自動）
# ルートから実行する場合:
cd /path/to/lichtblick
yarn install

# 3. サーバー起動
cd server
npm start

# または、モノレポルートから
yarn workspace @lichtblick/marketplace-server start
```

### サーバー起動確認

```bash
# ブラウザまたはcurlで確認
curl http://localhost:3000/extensions.json

# レスポンス例
{
  "version": "1.0.0",
  "lastUpdated": "2025-09-30T12:00:00Z",
  "extensions": [...]
}
```

### Lichtblickとの接続設定

Lichtblickアプリケーションの環境変数または設定ファイルで、マーケットプレイスURLを設定：

```bash
# 環境変数
export MARKETPLACE_URL=http://localhost:3000

# または設定ファイル
{
  "marketplace": {
    "url": "http://localhost:3000"
  }
}
```

## 🧪 テストデータ作成ガイドライン

### サンプル拡張機能の要件

各サンプル拡張機能は以下を含むべきです：

1. **最低2バージョン**（マルチバージョン機能のテスト用）
2. **異なるタグ**（フィルタリング機能のテスト用）
3. **README/CHANGELOG**（詳細ページ表示のテスト用）
4. **サムネイル画像**（UI表示のテスト用）

### サンプルデータの種類

推奨するサンプル拡張機能：

| 拡張機能名        | バージョン          | タグ                  | 用途                     |
| ----------------- | ------------------- | --------------------- | ------------------------ |
| example-extension | 1.0.0, 2.0.0        | demo, utilities       | 基本機能テスト           |
| data-visualizer   | 1.5.0, 2.1.0, 2.2.0 | visualization, charts | 複数バージョンテスト     |
| debug-tools       | 1.0.0               | debug, development    | 単一バージョンテスト     |
| ros-adapter       | 3.0.0, 3.1.0        | ros, robotics         | タグフィルタリングテスト |
| theme-dark        | 1.0.0, 1.1.0        | theme, ui             | UI表示テスト             |

### .foxeファイルの作成

開発段階では、ダミーの.foxeファイルで十分です：

```bash
# ダミーファイル作成例
echo "dummy extension content" > public/extensions/example-1.0.0.foxe
```

実際の.foxeファイルは、拡張機能開発時に追加します。

## 🔧 トラブルシューティング

### ポートが既に使用されている

**エラー**: `Error: listen EADDRINUSE: address already in use :::3000`

**解決方法**:

```bash
# 別のポートを使用
PORT=3001 npm start

# または既存プロセスを終了
lsof -ti:3000 | xargs kill -9
```

### CORSエラーが発生する

**エラー**: `Access to fetch at 'http://localhost:3000/extensions.json' from origin '...' has been blocked by CORS policy`

**解決方法**:

`server.js`のCORSヘッダー設定を確認：

```javascript
res.setHeader("Access-Control-Allow-Origin", "*");
res.setHeader("Access-Control-Allow-Methods", "GET, OPTIONS");
```

### ファイルが見つからない

**エラー**: `404 Not Found`

**解決方法**:

1. ファイルパスが正しいか確認
2. `public/`ディレクトリ内にファイルが存在するか確認
3. ファイル名の大文字小文字を確認（特にmacOS）

## 📦 S3へのデプロイ手順

### AWS CLIを使用したデプロイ

```bash
# AWS CLIインストール確認
aws --version

# S3バケット作成（初回のみ）
aws s3 mb s3://lichtblick-marketplace

# パブリックアクセス設定
aws s3api put-public-access-block \
  --bucket lichtblick-marketplace \
  --public-access-block-configuration \
  "BlockPublicAcls=false,IgnorePublicAcls=false,BlockPublicPolicy=false,RestrictPublicBuckets=false"

# ファイル同期
aws s3 sync server/public/ s3://lichtblick-marketplace/ --acl public-read

# CloudFront配信確認
# https://your-cloudfront-domain.cloudfront.net/extensions.json
```

### 注意事項

- ✅ `public/`ディレクトリ配下のみをアップロード
- ✅ `--acl public-read`でパブリック読み取り権限を付与
- ✅ URLは相対パスにしておく（ドメイン変更に対応）
- ✅ CloudFrontのキャッシュ無効化を忘れずに

## 🔗 関連ドキュメント

- [IMPLEMENTATION_ACTION_PLAN.md](../IMPLEMENTATION_ACTION_PLAN.md) - 全体実装計画
- [hybrid-version-data-structure.md](../architecture/hybrid-version-data-structure.md) - データ構造設計
- [MARKETPLACE_FEATURES.md](../MARKETPLACE_FEATURES.md) - 機能仕様
- [marketplace-api-specification.md](../planning/marketplace-api-specification.md) - API仕様（将来用）

## 📝 将来の拡張

### Phase 2以降で検討する機能

- [ ] **Honoサーバーの追加**: 動的API機能（検索、統計等）
- [ ] **認証機能**: 拡張機能アップロード時の認証
- [ ] **バージョン管理**: 自動バージョニング
- [ ] **統計機能**: ダウンロード数、人気度等
- [ ] **CDN最適化**: CloudFrontとの統合最適化
- [ ] **自動デプロイ**: CI/CDパイプライン構築

### Honoサーバー追加時の構成

```
server/
├── static-server.js      # 静的ファイル配信（現在のserver.js）
├── api-server.js         # Honoベースの動的API
├── package.json
└── public/
```

CloudFrontでのルーティング:

```
/api/*    → Lambda + Hono（動的API）
/*        → S3（静的ファイル）
```

---

**Document Version**: 2.0.0（完全版）
**Status**: ✅ 実装準備完了
**Next Action**: `server/`ディレクトリとファイル群の実装
**Maintained by**: Lichtblick Development Team

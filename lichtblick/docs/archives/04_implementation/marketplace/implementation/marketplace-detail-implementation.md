# マーケットプレイス詳細画面の新規実装 - 完了レポート

## 📋 実装概要

拡張機能とレイアウトの詳細画面を、OSSとは独立した新しい実装として作成しました。
これにより、OSSマージ時のコンフリクトを回避し、自由にカスタマイズできる構造になりました。

## 🎯 実装目標

- ✅ OSSファイル（`ExtensionDetails.tsx`, `LayoutDetails.tsx`）とは独立した実装
- ✅ 共通UIの統一（MarketplaceDetailBase）
- ✅ 拡張機能とレイアウトそれぞれの詳細画面を独自実装
- ✅ 自由にカスタマイズ可能な構造
- ✅ 既存機能との完全な互換性

## 📁 ファイル構成

### 新規作成されたファイル

```
packages/suite-base/src/
├── components/
│   ├── shared/
│   │   └── MarketplaceUI/
│   │       └── MarketplaceDetailBase.tsx         # 共通ベースコンポーネント (新規)
│   ├── ExtensionsSettings/
│   │   └── ExtensionDetail.tsx                    # 拡張機能詳細画面 (新規)
│   └── LayoutSettings/
│       └── LayoutDetail.tsx                       # レイアウト詳細画面 (新規)
```

### 更新されたファイル

```
packages/suite-base/src/
├── components/
│   ├── shared/
│   │   └── MarketplaceUI/
│   │       ├── types.ts                           # 型定義追加
│   │       └── index.ts                           # エクスポート追加
│   ├── ExtensionsSettings/
│   │   └── ExtensionMarketplaceSettings.tsx       # 新コンポーネント使用
│   └── LayoutMarketplaceSettings.tsx              # 新コンポーネント統合
```

## 🏗️ アーキテクチャ

### 1. MarketplaceDetailBase (共通ベースコンポーネント)

**場所**: `packages/suite-base/src/components/shared/MarketplaceUI/MarketplaceDetailBase.tsx`

**役割**: 拡張機能とレイアウトの詳細画面で共通利用できるベースコンポーネント

**主な機能**:

- ✅ ヘッダー部分（戻るボタン、タイトル）
- ✅ 基本情報表示（ID、バージョン、ライセンス、説明）
- ✅ タブUI（README、CHANGELOG）
- ✅ アクションボタンのレイアウト
- ✅ 拡張可能なスロット（extraInfoContent）

**Props**:

```typescript
interface MarketplaceDetailBaseProps {
  // ヘッダー情報
  title: string;
  onClose: () => void;

  // 基本情報
  id: string;
  version: string;
  license?: string;
  publisher: string;
  description: string;
  homepage?: string;

  // アクションボタン
  actionButton?: ReactNode;

  // カスタムコンテンツ
  extraInfoContent?: ReactNode;

  // タブ設定
  tabs: TabConfig[];
  defaultTab?: number;
}
```

### 2. ExtensionDetail (拡張機能詳細画面)

**場所**: `packages/suite-base/src/components/ExtensionsSettings/ExtensionDetail.tsx`

**役割**: 拡張機能専用の詳細画面（新規独自実装）

**主な機能**:

- ✅ 拡張機能の詳細情報表示
- ✅ README/CHANGELOGの表示
- ✅ インストール/アンインストール機能
- ✅ バージョン情報の表示
- ✅ セキュリティ（SHA256ハッシュ検証）
- ✅ アナリティクスイベント送信

**ExtensionDetails.tsx との違い**:
| 項目 | ExtensionDetails.tsx (OSS) | ExtensionDetail.tsx (新規) |
|------|---------------------------|---------------------------|
| 実装パス | `components/ExtensionDetails.tsx` | `components/ExtensionsSettings/ExtensionDetail.tsx` |
| ベース | 直接実装 | MarketplaceDetailBase使用 |
| コンフリクト | OSSマージで発生する可能性 | 独立しているため発生しない |
| カスタマイズ | 制限あり | 自由に可能 |

### 3. LayoutDetail (レイアウト詳細画面)

**場所**: `packages/suite-base/src/components/LayoutSettings/LayoutDetail.tsx`

**役割**: レイアウト専用の詳細画面（新規独自実装）

**主な機能**:

- ✅ レイアウトの詳細情報表示
- ✅ サムネイル画像の表示
- ✅ タグ一覧の表示
- ✅ 統計情報（ダウンロード数、評価）の表示
- ✅ README/CHANGELOGの表示
- ✅ インストール/アンインストール機能

**LayoutDetails.tsx との違い**:
| 項目 | LayoutDetails.tsx (OSS) | LayoutDetail.tsx (新規) |
|------|------------------------|------------------------|
| 実装パス | `components/LayoutDetails.tsx` | `components/LayoutSettings/LayoutDetail.tsx` |
| ベース | 直接実装 | MarketplaceDetailBase使用 |
| コンフリクト | OSSマージで発生する可能性 | 独立しているため発生しない |
| カスタマイズ | 制限あり | 自由に可能 |
| 独自機能 | - | サムネイル、タグ、統計情報 |

## 🔄 データフロー

### 拡張機能詳細画面

```
ExtensionMarketplaceSettings
  ↓ (カードクリック)
  ↓ setFocusedExtension()
  ↓
ExtensionDetail
  ↓ (MarketplaceDetailBaseを使用)
  ↓
  ├─ ExtensionCatalog (インストール/アンインストール)
  ├─ ExtensionMarketplace (README/CHANGELOG取得)
  └─ Analytics (イベント送信)
```

### レイアウト詳細画面

```
LayoutMarketplaceSettings
  ↓ (カードクリック)
  ↓ setFocusedLayout()
  ↓
LayoutDetail
  ↓ (MarketplaceDetailBaseを使用)
  ↓
  ├─ LayoutMarketplace (レイアウトデータ取得)
  └─ LayoutCatalog (インストール/アンインストール)
```

## 🎨 UI設計

### 共通レイアウト

```
┌─────────────────────────────────────────────────────────┐
│ ← Back                                                  │
│ Extension/Layout Name                                   │
├─────────────────────────────────────────────────────────┤
│ publisher.extension-name  v1.0.0  MIT                  │
│ Publisher Name                                          │
│ Description text...                                     │
│                                                         │
│ [Extra Info Content - サムネイル、タグ、統計など]          │
│                                                         │
│ [Install/Uninstall Button]                             │
├─────────────────────────────────────────────────────────┤
│ [README] [CHANGELOG]                                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Tab Content (Markdown)                                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### レイアウト固有の追加要素

- **サムネイル**: レイアウトのプレビュー画像
- **タグ**: Chip コンポーネントで表示
- **統計情報**: ダウンロード数、評価（星マーク）

## 📊 型定義

### 追加された型

**場所**: `packages/suite-base/src/components/shared/MarketplaceUI/types.ts`

```typescript
/**
 * Operation status for install/uninstall actions
 */
export enum OperationStatus {
  IDLE = "idle",
  INSTALLING = "installing",
  UNINSTALLING = "uninstalling",
}

/**
 * Detail tab configuration
 */
export interface DetailTabConfig {
  label: string;
  content: React.ReactNode;
}
```

## ✅ 実装完了項目

1. ✅ **共通ベースコンポーネント作成**

   - MarketplaceDetailBase.tsx
   - 再利用可能な詳細画面UI

2. ✅ **拡張機能詳細画面作成**

   - ExtensionDetail.tsx
   - OSSとは独立した実装

3. ✅ **レイアウト詳細画面作成**

   - LayoutDetail.tsx
   - サムネイル、タグ、統計情報表示

4. ✅ **マーケットプレイス画面統合**

   - ExtensionMarketplaceSettings の更新
   - LayoutMarketplaceSettings の更新

5. ✅ **型定義の整理**

   - OperationStatus enum
   - DetailTabConfig interface

6. ✅ **エラー修正**
   - 全ファイルでLintエラーなし

## 🚀 使用方法

### 拡張機能詳細画面

```tsx
import ExtensionDetail from "@lichtblick/suite-base/components/ExtensionsSettings/ExtensionDetail";

<ExtensionDetail
  installed={isInstalled}
  extension={extensionDetail}
  onClose={() => setShowDetail(false)}
/>;
```

### レイアウト詳細画面

```tsx
import LayoutDetail from "@lichtblick/suite-base/components/LayoutSettings/LayoutDetail";

<LayoutDetail installed={isInstalled} layout={layoutDetail} onClose={() => setShowDetail(false)} />;
```

### カスタム詳細画面の作成

```tsx
import { MarketplaceDetailBase } from "@lichtblick/suite-base/components/shared/MarketplaceUI";

<MarketplaceDetailBase
  title="Custom Item"
  onClose={handleClose}
  id="custom.item"
  version="1.0.0"
  license="MIT"
  publisher="Publisher"
  description="Description..."
  homepage="https://example.com"
  actionButton={<Button>Custom Action</Button>}
  extraInfoContent={<div>Custom Content</div>}
  tabs={[
    { label: "Tab1", content: "Content1" },
    { label: "Tab2", content: "Content2" },
  ]}
/>;
```

## 🔧 今後の拡張可能性

### 1. バージョン切り替え機能

- 詳細画面内で複数バージョンを切り替え
- バージョンごとのREADME/CHANGELOG表示

### 2. 依存関係表示

- 拡張機能の依存関係グラフ
- レイアウトが必要とする拡張機能の表示

### 3. レビュー・評価機能

- ユーザーレビューの表示
- 評価の投稿・編集

### 4. 更新通知

- 新バージョンの通知
- 自動更新機能

### 5. スクリーンショット

- 複数画像のギャラリー表示
- 拡大表示機能

## 📝 注意事項

### OSSとの共存

- **既存ファイルは保持**: `ExtensionDetails.tsx`、`LayoutDetails.tsx`は削除していません
- **独立した実装パス**: 新しいコンポーネントは別のディレクトリに配置
- **段階的な移行**: 必要に応じて旧コンポーネントから新コンポーネントへ移行可能

### マージ戦略

```
OSS更新時:
  ExtensionDetails.tsx  → OSSの更新を受け入れる（コンフリクトなし）
  LayoutDetails.tsx     → OSSの更新を受け入れる（コンフリクトなし）

独自実装:
  ExtensionDetail.tsx   → 自由にカスタマイズ可能
  LayoutDetail.tsx      → 自由にカスタマイズ可能
  MarketplaceDetailBase → 共通UIの改善
```

## 🎉 まとめ

この実装により、以下の目標を達成しました:

1. ✅ **OSSとの独立性**: マージ時のコンフリクトを回避
2. ✅ **共通UIの統一**: MarketplaceDetailBaseによる一貫性
3. ✅ **カスタマイズ性**: 自由に拡張可能な構造
4. ✅ **互換性**: 既存機能との完全な互換性
5. ✅ **保守性**: 明確なコンポーネント分離

拡張機能とレイアウトの詳細画面が、独立した実装として自由にカスタマイズできるようになりました。

---

**実装日**: 2025年10月1日
**実装者**: GitHub Copilot
**ステータス**: ✅ 完了

# Rustフロントエンドフレームワーク比較 - Lichtblick完全再現

**作成日**: 2025-01-XX
**バージョン**: 1.0.0
**対象**: Lichtblick完全Rust移行プロジェクト

---

## 📋 目次

1. [概要](#概要)
2. [要件定義](#要件定義)
3. [フレームワーク比較](#フレームワーク比較)
4. [詳細評価](#詳細評価)
5. [推奨フレームワーク](#推奨フレームワーク)
6. [実装例](#実装例)
7. [移行戦略](#移行戦略)

---

## 概要

Lichtblickを完全にRustで再現するにあたり、フロントエンドフレームワークの選定は最重要決定事項です。このドキュメントでは、主要なRust UIフレームワークを詳細に比較し、Lichtblickの要件に最適な選択を推奨します。

### LichtblickのUI要件

- **複雑なUI**: 20+パネル、動的レイアウト、設定ツリー
- **3D可視化統合**: wgpuとの統合が必須
- **クロスプラットフォーム**: Web、Desktop（Windows、macOS、Linux）
- **パフォーマンス**: 大量データの可視化、リアルタイム更新
- **拡張性**: 拡張機能システムとの統合

---

## 要件定義

### 必須要件

1. **完全なRust実装**: Web技術（React等）は使用しない
2. **wgpu統合**: 3D可視化エンジンとの統合が容易
3. **クロスプラットフォーム**: Web + Desktop対応
4. **パフォーマンス**: 60fps以上のレンダリング
5. **成熟度**: 実プロダクションで使用可能

### 優先要件

1. **学習曲線**: チームが習得しやすい
2. **エコシステム**: 豊富なライブラリとコミュニティ
3. **開発速度**: 開発効率が高い
4. **カスタマイズ性**: UIの自由度が高い

---

## フレームワーク比較

### 1. egui ⭐最有力候補

**公式**: https://github.com/emilk/egui
**バージョン**: 0.27+
**ライセンス**: Apache-2.0 / MIT

#### 特徴

- **イミディエイトモードUI**: 毎フレームUIを描画
- **wgpu統合**: ネイティブサポート
- **クロスプラットフォーム**: Web、Desktop、モバイル対応
- **軽量**: 依存関係が少ない

#### アーキテクチャ

```rust
use eframe::egui;

struct MyApp {
    // アプリケーション状態
}

impl eframe::App for MyApp {
    fn update(&mut self, ctx: &egui::Context, frame: &mut eframe::Frame) {
        egui::CentralPanel::default().show(ctx, |ui| {
            ui.heading("Lichtblick");
            // UI構築
        });
    }
}
```

#### メリット

✅ **wgpu統合が容易**: eguiはwgpuを直接使用
✅ **成熟度が高い**: 多くのプロジェクトで使用実績あり
✅ **パフォーマンス**: イミディエイトモードは複雑なUIに適している
✅ **学習曲線**: 比較的シンプルなAPI
✅ **カスタマイズ性**: テーマシステムが充実
✅ **Web対応**: `eframe`でWebAssembly対応

#### デメリット

⚠️ **イミディエイトモードの制約**: 毎フレーム再描画が必要
⚠️ **Webパフォーマンス**: WebAssemblyでのパフォーマンス制限
⚠️ **UIライブラリ**: Material-UIのような豊富なコンポーネントがない

#### Lichtblick適合度: ⭐⭐⭐⭐⭐ (5/5)

**理由**:

- wgpuとの統合が最も容易
- 複雑なUI（パネルシステム）に適している
- 成熟度が高く、実プロダクションで使用可能

---

### 2. iced

**公式**: https://github.com/iced-rs/iced
**バージョン**: 0.12+
**ライセンス**: MIT

#### 特徴

- **Elm風アーキテクチャ**: 関数型、宣言的UI
- **クロスプラットフォーム**: Desktop、Web対応
- **wgpu統合**: 可能だが設定が必要

#### アーキテクチャ

```rust
use iced::{Application, Command, Element, Settings};

#[derive(Debug, Clone)]
enum Message {
    ButtonPressed,
}

struct MyApp {
    // 状態
}

impl Application for MyApp {
    type Message = Message;
    type Theme = iced::Theme;
    type Executor = iced::executor::Default;

    fn new(_flags: ()) -> (Self, Command<Message>) {
        (MyApp {}, Command::none())
    }

    fn title(&self) -> String {
        "Lichtblick".to_string()
    }

    fn update(&mut self, message: Message) -> Command<Message> {
        match message {
            Message::ButtonPressed => {
                // 状態更新
            }
        }
        Command::none()
    }

    fn view(&self) -> Element<Message> {
        // UI構築
    }
}
```

#### メリット

✅ **宣言的UI**: React風の書きやすさ
✅ **型安全性**: Rustの型システムを最大限活用
✅ **クロスプラットフォーム**: Desktop、Web対応

#### デメリット

❌ **wgpu統合が複雑**: カスタムレンダラーが必要
❌ **学習曲線**: Elm風アーキテクチャの理解が必要
❌ **成熟度**: eguiより若い
❌ **3D可視化統合**: 実装が複雑

#### Lichtblick適合度: ⭐⭐⭐ (3/5)

**理由**:

- wgpu統合が複雑
- 3D可視化との統合が困難
- 学習曲線が高い

---

### 3. Dioxus

**公式**: https://github.com/DioxusLabs/dioxus
**バージョン**: 0.5+
**ライセンス**: MIT

#### 特徴

- **React風**: JSXライクな構文
- **クロスプラットフォーム**: Web、Desktop、モバイル
- **Tauri統合**: ネイティブサポート

#### アーキテクチャ

```rust
use dioxus::prelude::*;

fn App(cx: Scope) -> Element {
    cx.render(rsx! {
        div {
            h1 { "Lichtblick" }
            // UI構築
        }
    })
}
```

#### メリット

✅ **React風**: 既存のReact知識を活用可能
✅ **クロスプラットフォーム**: 幅広いプラットフォーム対応
✅ **Tauri統合**: デスクトップアプリに最適

#### デメリット

❌ **wgpu統合が困難**: Web技術ベースのため
❌ **完全なRust実装ではない**: DOMベースのレンダリング
❌ **3D可視化**: WebGL経由になるためパフォーマンス制限

#### Lichtblick適合度: ⭐⭐ (2/5)

**理由**:

- wgpuとの直接統合が困難
- 完全なRust実装ではない（DOMベース）
- 3D可視化のパフォーマンスが制限される

---

### 4. Slint

**公式**: https://github.com/slint-ui/slint
**バージョン**: 1.7+
**ライセンス**: GPL-3.0 / 商用ライセンス

#### 特徴

- **QML風**: 宣言的UI言語
- **デスクトップ向け**: 主にDesktopアプリ
- **Rust統合**: Rustバックエンドと統合

#### アーキテクチャ

```slint
// MainWindow.slint
import { Button } from "std-widgets.slint";

export component MainWindow inherits Window {
    Button {
        text: "Click me";
        clicked => {
            // イベント処理
        }
    }
}
```

```rust
// main.rs
use slint::slint;

slint::include_modules!();

fn main() {
    let ui = MainWindow::new().unwrap();
    ui.run().unwrap();
}
```

#### メリット

✅ **デスクトップ最適化**: ネイティブパフォーマンス
✅ **商用サポート**: 企業向けサポートあり

#### デメリット

❌ **Web対応が限定的**: WebAssemblyサポートは実験的
❌ **wgpu統合が困難**: カスタムレンダラーが必要
❌ **ライセンス**: 商用利用にはライセンス費用
❌ **学習曲線**: 独自の言語を学習する必要

#### Lichtblick適合度: ⭐⭐ (2/5)

**理由**:

- Web対応が限定的
- wgpu統合が困難
- ライセンスの問題

---

### 5. Relm4

**公式**: https://github.com/Relm4/Relm4
**バージョン**: 0.7+
**ライセンス**: Apache-2.0 / MIT

#### 特徴

- **GTK4ベース**: Linux向け
- **Elm風**: 関数型アーキテクチャ
- **デスクトップ向け**: 主にLinux

#### メリット

✅ **GTK4統合**: LinuxネイティブUI
✅ **Elm風**: 宣言的UI

#### デメリット

❌ **プラットフォーム限定**: 主にLinux
❌ **Web対応なし**: Webアプリには不適切
❌ **wgpu統合が困難**: GTK4との統合が必要

#### Lichtblick適合度: ⭐ (1/5)

**理由**:

- クロスプラットフォーム要件を満たさない
- Web対応がない

---

## 詳細評価

### 評価マトリックス

| フレームワーク | wgpu統合   | Web対応    | Desktop対応 | 成熟度     | 学習曲線   | パフォーマンス | 総合評価       |
| -------------- | ---------- | ---------- | ----------- | ---------- | ---------- | -------------- | -------------- |
| **egui**       | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐   | ⭐⭐⭐⭐⭐  | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐   | ⭐⭐⭐⭐⭐     | **⭐⭐⭐⭐⭐** |
| **iced**       | ⭐⭐⭐     | ⭐⭐⭐⭐   | ⭐⭐⭐⭐⭐  | ⭐⭐⭐     | ⭐⭐⭐     | ⭐⭐⭐⭐       | ⭐⭐⭐         |
| **Dioxus**     | ⭐⭐       | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐    | ⭐⭐⭐     | ⭐⭐⭐⭐⭐ | ⭐⭐⭐         | ⭐⭐           |
| **Slint**      | ⭐⭐       | ⭐⭐       | ⭐⭐⭐⭐⭐  | ⭐⭐⭐⭐   | ⭐⭐       | ⭐⭐⭐⭐       | ⭐⭐           |
| **Relm4**      | ⭐         | ⭐         | ⭐⭐⭐      | ⭐⭐⭐     | ⭐⭐⭐     | ⭐⭐⭐         | ⭐             |

### 機能別比較

#### 1. wgpu統合の容易さ

**egui**: ✅✅✅✅✅

- eguiはwgpuを直接使用
- 3D可視化との統合が最も容易
- カスタムレンダリングが簡単

**iced**: ⚠️⚠️⚠️

- カスタムレンダラーが必要
- 実装が複雑

**Dioxus**: ❌❌

- DOMベースのため統合が困難

#### 2. パネルシステムの実装

**egui**: ✅✅✅✅✅

- イミディエイトモードは動的UIに最適
- パネルの動的追加・削除が容易

**iced**: ⚠️⚠️⚠️

- 状態管理が複雑
- 動的UIの実装が困難

**Dioxus**: ⚠️⚠️

- React風だが、wgpu統合が困難

#### 3. クロスプラットフォーム対応

**egui**: ✅✅✅✅

- WebAssembly対応
- Desktop（Windows、macOS、Linux）対応
- モバイル対応も可能

**iced**: ✅✅✅✅

- Web対応あり
- Desktop対応

**Dioxus**: ✅✅✅✅✅

- 最も幅広いプラットフォーム対応

---

## 推奨フレームワーク

### 🏆 推奨: egui

**理由**:

1. **wgpu統合が最も容易**

   - eguiはwgpuを直接使用
   - 3D可視化エンジンとの統合が簡単
   - カスタムレンダリングが可能

2. **Lichtblickの要件に最適**

   - 複雑なUI（パネルシステム）に適している
   - 動的レイアウト管理が容易
   - 設定ツリーシステムの実装が簡単

3. **成熟度が高い**

   - 多くのプロジェクトで使用実績
   - 活発なコミュニティ
   - 安定したAPI

4. **パフォーマンス**

   - ネイティブパフォーマンス
   - 60fps以上のレンダリングが可能

5. **クロスプラットフォーム**
   - Web、Desktop対応
   - 実装が統一されている

### アーキテクチャ設計

```
┌─────────────────────────────────────────┐
│         Lichtblick Application          │
├─────────────────────────────────────────┤
│  egui (UI Framework)                   │
│  ├── Panel System                       │
│  ├── Layout Management                  │
│  ├── Settings Tree                      │
│  └── Extension UI                       │
├─────────────────────────────────────────┤
│  wgpu (3D Rendering)                   │
│  ├── Scene Management                   │
│  ├── Point Cloud Renderer               │
│  ├── Mesh Renderer                      │
│  └── URDF Renderer                      │
├─────────────────────────────────────────┤
│  Rust Backend                           │
│  ├── Data Source Manager                │
│  ├── Player System                      │
│  └── Message Pipeline                   │
└─────────────────────────────────────────┘
```

### egui + wgpu統合パターン

eguiとwgpuを統合する方法：

1. **eguiのカスタムレンダリング**

   - eguiの`Custom3D`ウィジェットを使用
   - wgpuコンテキストを共有

2. **別ウィンドウ/パネル**
   - 3D可視化用の専用パネル
   - eguiのパネルシステムに統合

---

## 実装例

### eguiでのパネルシステム実装

```rust
use eframe::egui;
use std::collections::HashMap;

#[derive(Default)]
struct LichtblickApp {
    panels: HashMap<String, Box<dyn Panel>>,
    layout: LayoutManager,
    message_pipeline: MessagePipeline,
}

impl eframe::App for LichtblickApp {
    fn update(&mut self, ctx: &egui::Context, frame: &mut eframe::Frame) {
        // メニューバー
        egui::TopBottomPanel::top("menu_bar").show(ctx, |ui| {
            egui::menu::bar(ui, |ui| {
                ui.menu_button("File", |ui| {
                    if ui.button("Open Data Source").clicked() {
                        // データソース選択
                    }
                });
            });
        });

        // サイドバー
        egui::SidePanel::left("left_panel")
            .resizable(true)
            .default_width(200.0)
            .show(ctx, |ui| {
                ui.heading("Panels");
                for (id, panel) in &self.panels {
                    if ui.button(panel.title()).clicked() {
                        // パネルを開く
                    }
                }
            });

        // 中央パネルエリア
        egui::CentralPanel::default().show(ctx, |ui| {
            // レイアウトに基づいてパネルを表示
            self.layout.render(ui, &mut self.panels, &self.message_pipeline);
        });

        // 3D可視化パネル
        if let Some(three_dee_panel) = self.panels.get_mut("three_dee") {
            three_dee_panel.update(&self.message_pipeline);
        }
    }
}

trait Panel {
    fn title(&self) -> &str;
    fn render(&mut self, ui: &mut egui::Ui, pipeline: &MessagePipeline);
    fn update(&mut self, pipeline: &MessagePipeline);
}
```

### wgpu統合例

```rust
use egui::*;
use wgpu::*;

struct ThreeDeePanel {
    renderer: wgpu::Renderer,
    scene: Scene,
}

impl Panel for ThreeDeePanel {
    fn title(&self) -> &str {
        "3D Visualization"
    }

    fn render(&mut self, ui: &mut egui::Ui, pipeline: &MessagePipeline) {
        // eguiのカスタム3Dウィジェット
        egui::Frame::canvas(ui.style()).show(ui, |ui| {
            let (rect, response) = ui.allocate_exact_size(
                ui.available_size(),
                egui::Sense::click_and_drag(),
            );

            // wgpuレンダリング
            self.renderer.render(&mut self.scene, rect);
        });
    }

    fn update(&mut self, pipeline: &MessagePipeline) {
        // メッセージから3Dオブジェクトを更新
        let messages = pipeline.get_messages(&["/point_cloud", "/pose"]);
        self.scene.update(messages);
    }
}
```

### パネルシステムの実装

```rust
use eframe::egui;

pub struct PanelManager {
    panels: HashMap<String, Box<dyn Panel>>,
    active_panel: Option<String>,
}

impl PanelManager {
    pub fn new() -> Self {
        let mut manager = Self {
            panels: HashMap::new(),
            active_panel: None,
        };

        // パネルを登録
        manager.register_panel("plot", Box::new(PlotPanel::new()));
        manager.register_panel("image", Box::new(ImagePanel::new()));
        manager.register_panel("three_dee", Box::new(ThreeDeePanel::new()));
        // ...

        manager
    }

    pub fn register_panel(&mut self, id: String, panel: Box<dyn Panel>) {
        self.panels.insert(id, panel);
    }

    pub fn render_panel(&mut self, id: &str, ui: &mut egui::Ui, pipeline: &MessagePipeline) {
        if let Some(panel) = self.panels.get_mut(id) {
            panel.render(ui, pipeline);
        }
    }
}
```

---

## 移行戦略

### Phase 1: egui基盤構築（1-2ヶ月）

1. **eguiセットアップ**

   - `eframe`プロジェクトの初期化
   - 基本的なUI構造の実装
   - テーマシステムの設定

2. **パネルシステムの実装**
   - パネル抽象化の定義
   - 基本的なパネル（RawMessages等）の実装
   - レイアウト管理システム

### Phase 2: wgpu統合（2-3ヶ月）

1. **wgpuレンダラーの実装**

   - wgpuコンテキストの初期化
   - eguiとの統合
   - 基本的な3Dシーンの表示

2. **3Dパネルの実装**
   - ThreeDeeRenderパネルの実装
   - カメラ制御
   - 点群レンダリング

### Phase 3: パネル拡張（2-3ヶ月）

1. **主要パネルの実装**

   - Plotパネル
   - Imageパネル
   - Mapパネル
   - Logパネル

2. **UI改善**
   - 設定ツリーシステム
   - ドラッグ&ドロップ
   - キーボードショートカット

---

## 結論

### 最終推奨: **egui**

Lichtblickの完全Rust再現には、**egui**が最適な選択です。

**主な理由**:

1. ✅ **wgpu統合が最も容易** - 3D可視化エンジンとの統合が簡単
2. ✅ **要件に最適** - 複雑なUI、パネルシステムに適している
3. ✅ **成熟度が高い** - 実プロダクションで使用可能
4. ✅ **パフォーマンス** - ネイティブパフォーマンス
5. ✅ **クロスプラットフォーム** - Web、Desktop対応

### 次のステップ

1. **eguiプロトタイプの作成**

   - 基本的なUI構造
   - パネルシステムのプロトタイプ
   - wgpu統合の検証

2. **技術検証**

   - パフォーマンステスト
   - 3D可視化の統合テスト
   - Web対応の検証

3. **開発開始**
   - プロジェクトセットアップ
   - アーキテクチャ設計の詳細化
   - 実装開始

---

## 参考資料

- [egui Documentation](https://docs.rs/egui/)
- [egui Examples](https://github.com/emilk/egui/tree/master/examples)
- [wgpu Documentation](https://wgpu.rs/)
- [egui + wgpu Integration](https://github.com/emilk/egui/tree/master/crates/egui-wgpu)

---

**作成者**: AI Assistant
**レビュー**: 要レビュー
**承認**: 未承認
